{
  "name": "react-web-example",
  "version": "0.0.1",
  "description": "example",
  "author": "",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "setup": "js-config-gen -i react-web",
    "test": "echo \"Error: no test specified\" && exit 1",
    "remove": "rm -rf ./node_modules",
    "reinstall": "npm run remove && npm install"
  },
  "devDependencies": {
    "@mikechau/js-config-gen": "file:../../"
  },
  "dependencies": {
  }
}
