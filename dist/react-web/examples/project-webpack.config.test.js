// AUTOGENERATED BY @mikechau/js-config-gen.
// Sun Dec 20 2015 20:27:13 GMT-0600 (CST)

'use strict';

var _ = require('lodash');
var path = require('path');
var webpack = require('webpack');
var baseWebpackConfig = require('./node_modules/@mikechau/js-config-gen/dist/react-web/base-webpack.config.test.js');

var config = _.merge({}, baseWebpackConfig, {
  module: {
    loaders: baseWebpackConfig.module.loaders.concat([
      {
        "test": /\.css$/,
        "loader": "style!css"
      },
      {
        "test": /\.less$/,
        "loader": "style!css!less"
      },
      {
        "test": /\.scss$/,
        "loader": "style!css!scss"
      },
    ])
  },

  plugins: [
    new webpack.DefinePlugin({
      'process.env': {
        NODE_ENV: JSON.stringify(process.env.NODE_ENV || 'test')
      }
    }),

    new webpack.NoErrorsPlugin()
  ]
});

module.exports = config;
